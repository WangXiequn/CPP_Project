#include <bits/stdc++.h>
#include "Matrix.h"
using namespace std;
template<typename T,typename T2>
static void foo(T t1) {
    if (std::is_same<T, complex<double>>::value){
        cout<<"Yes";
    }
}
int main() {
    vector<vector<double>> l;
    vector<vector<double>> l1;
    vector<double> temp;
    l.push_back(temp);
    l[0].push_back(1);
    l1.push_back(temp);

}